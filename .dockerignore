# 全イメージ共通で不要なもの（next / server イメージは context: . でビルドされ、
# この .dockerignore が適用される。next/ や server-ts/ は両イメージのどちらかで
# 必要なので除外しない＝代わりに重い生成物だけをピンポイントで除外する）
**/node_modules
**/.next
**/dist
.drizzle/
.git
.github/
server-rs/
.playwright-mcp/
init_db/
*.zip
**/.DS_Store
compose*.yaml
docker-compose*.yml
README.md
TASKS.md
next/src/stories/
next/.storybook/
